name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxcursor-dev libxrandr-dev \
              libxi-dev libgl1-mesa-dev libudev-dev pkg-config

      - uses: dtolnay/rust-toolchain@stable

      - name: Build (Debug)
        run: cargo build

      - name: Test
        run: cargo test

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Build (Debug)
        run: cargo build

      - name: Test
        run: cargo test

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - name: Build (Debug)
        run: cargo build

      - name: Test
        run: cargo test
